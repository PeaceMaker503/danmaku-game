using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace gameLIB.components.stage
{
  //  public delegate void Spell(SpellInfo si);
    public sealed class RunOnceAction
    {
      /*  private readonly Action _F;
        private readonly Spell _S;
        private SpellInfo _si;
        private bool _hasRun;

        public RunOnceAction(Action f)
        {
            _F = f;
        }

        public RunOnceAction(Spell s, SpellInfo si)
        {
            _S = s;
            _si = si;
        }

        public void run()
        {
            if (_hasRun) return;
            _F();
            _hasRun = true;
        }

        public void runSpell()
        {
            if (_hasRun) return;
            _S(_si);
            _hasRun = true;
        }

        public void init()
        {
            _hasRun = false;
        }*/
    }
}
